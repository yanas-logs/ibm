[package]
name = "desktop"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { workspace = true, features = ["router", "desktop", "fullstack"] }
ui = { workspace = true }

[features]
default = ["dioxus/desktop"]
server = ["ui/server"]